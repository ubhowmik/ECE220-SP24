#include <stdio.h>
#include <string.h>
#define SIZE_BUF 50
int main()
{
fgets(buf, SIZE_BUF, stdin);
printf("%s\n", buf);
gets(buf);
printf("%s\n", buf);
    scanf("%s",buf);
    printf("%s\n", buf);
 
}